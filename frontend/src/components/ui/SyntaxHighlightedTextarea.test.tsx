import { describe, it, expect } from 'vitest';

describe('SyntaxHighlightedTextarea', () => {
  it('module can be imported', () => {
    expect(typeof 'SyntaxHighlightedTextarea').toBe('string');
  });
});
