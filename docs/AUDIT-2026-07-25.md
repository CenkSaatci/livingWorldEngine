# LWE Audit — Stand 2026-07-25

> Audit nach der manuellen Test-Session. Ziel: aktuelle Code-Qualität, offene Punkte und Empfehlungen.

---

## 1. Zusammenfassung

| Bereich | Status |
|---|---|
| Backend-Tests | ✅ 220 Tests, 0 Failures |
| Frontend-Tests | ✅ 43 Dateien, 105 Tests |
| Build (Frontend) | ✅ Fehlerfrei |
| Manueller Test (SystemWizard) | ✅ Grundlegend funktional |
| Uncommittete Änderungen | ⚠️ 50 Dateien, 2064 Insertions |

**Gesamturteil:** Der Stand ist lauffähig und grundlegend getestet, aber es gibt **50 uncommittete Dateien** mit Änderungen aus Phasen 17-21 plus Session-Fixes. Ein Commit sollte zeitnah erfolgen.

---

## 2. Was in dieser Session gefixt wurde

### Bugfixes (Character Creation & Sheet)

| # | Bug | Status |
|---|---|---|
| 1 | Kein CharacterSheet nach PC-Erstellung | ✅ `navigate('/characters/{id}')` |
| 2 | Level immer 0 | ✅ `LevelUpService.getLevel()` aufgerufen |
| 3 | FormulaOverrides nie geladen | ✅ `metadata_json` → `metadataJson` |
| 4 | Entity-Liste leere Spalten | ✅ Interface auf camelCase |
| 5 | NpcViewPage Error #31 | ✅ `NpcData` Interface camelCase |
| 6 | Falscher API-Call in NpcViewPage | ✅ `/entities/{id}` → `/worlds/{w}/entities/{id}` |
| 7 | useApiGet leere URL → 500 | ✅ `if (!url) return` |
| 8 | Error-Objekt als React-Child | ✅ typeof-Check vor setError |
| 9 | PC/NPC-Routing | ✅ PC → CharacterSheet, NPC → NpcViewPage |

### Phase 21: Combat-Schema

| Task | Status |
|---|---|
| P21-T01: Schema (critical_hit, saving_throws, resting) | ✅ |
| P21-T02: SystemWizard Combat-Step | ✅ |
| P21-T03: Resting-Backend (short/long) | ✅ |

### Neue Phasen definiert

| Phase | Inhalt | Status |
|---|---|---|
| Phase 22 | Social Mechanics (Konzept) | 🔜 |
| Phase 23 | Items, Abilities & Damage System | 🔜 (5 Tasks) |

---

## 3. Offene Punkte / Bekannte Probleme

### Kritisch (sollte vor Release gefixt werden)

| Problem | Datei | Schwere |
|---|---|---|
| **Keine Attribut-Initialisierung beim Entity-Create** — Formular sendet kein `attributesJson` | `EntityCreateModal.tsx` | 🟡 (Workaround: Sheet zeigt Defaults) |
| **Location-Loading N+1** — Sequentielles Laden aller Regionen→Locations | `EntityCreateModal.tsx:57-73` | 🟡 |
| **NpcViewPage XP-Button** — Ruft `/entities/{id}/xp` auf, Endpoint existiert nicht | `NpcViewPage.tsx` | 🟡 |
| **5 leere catch-Blöcke** ohne Toast | `EntityCreateModal.tsx` | 🟡 |

### Mittel (nächste Phasen)

| Problem | Details |
|---|---|
| `OverrideInput` als nested Component in `FormulaOverrides` | Anti-Pattern, remountet bei jedem Render |
| `skillOverrides` Effect läuft bei jedem Fetch | Neue Objekt-Referenzen → unnötige Re-Renders |
| `useSheet` kein Cancel auf unmount | Potenzielles React-Warning |
| Doppelte `updateProgression`-Endpoints | `CharacterSheetController` + `EntityController` |

### Dokumentations-Lücken

| Datei | Lücke |
|---|---|
| `docs/API.md` | `rest/short`, `rest/long` nicht dokumentiert |
| `docs/API.md` | `PATCH /entities/{id}/skills` Response nicht dokumentiert |

---

## 4. Empfehlungen (Priorisiert)

### Sofort
1. **Commit der 50 Dateien** — der aktuelle Stand sollte gesichert werden
2. `docs/TASKS.md` — Phase 21 als ✅ markieren, Session-Fixes dokumentieren

### Phase 19/20-Nacharbeit
3. `NpcViewPage` XP-Button auf korrekten Endpoint fixen
4. `EntityCreateModal` N+1 Location-Loading optimieren (Backend-Batch-Endpoint)
5. Toast-Feedback in leere catch-Blöcke

### Architektur (Phase 23+)
6. `LevelUpService.getLevel()` in CharacterSheet als Cache
7. SystemWizard JSON-Mapping (buildRulesJson/parseRulesToWizard) durch geteilte Typen vereinfachen
8. `new ObjectMapper()` → Spring-Bean (9 Vorkommen)

---

## 5. Test-Status

```
Backend:  220 Tests, 0 Failures, 0 Errors
Frontend:  43 Files, 105 Tests, alle grün
```

### Manuelle Tests abgeschlossen

- [x] SystemWizard: System erstellen, speichern, exportieren
- [x] SystemWizard: Critical Hits, Saving Throws, Resting speicherbar
- [x] SystemWizard: Import/Export funktioniert
- [x] Character erstellen (PC) → navigiert zu CharacterSheet
- [x] CharacterSheet: Attribute, Skills, Abilities sichtbar
- [x] Entity-Liste: PC/NPC-Unterscheidung korrekt

### Noch zu testen

- [ ] Combat (Kampf starten, Aktionen, AP)
- [ ] Inventory (Drag & Drop)
- [ ] Resting (short/long via API)
- [ ] Character Export/Import Roundtrip
- [ ] Sicherheit (fremder User → 403)

---

## 6. Infrastruktur

| Komponente | Port | Status |
|---|---|---|
| Backend (Spring Boot) | 8080 | Läuft (PID wechselt, siehe /tmp/backend.log) |
| Frontend (serve, statisch) | 3000 | Läuft |

**Hinweis:** Der Vite-Dev-Server (5173) wurde durch statisches `serve` der Production-Builds ersetzt, da der Dev-Server mehrfach abstürzte. Für Entwicklung: `VITE_API_URL=http://192.168.31.151:8080/api/v1 npm run dev -- --host 0.0.0.0`.
